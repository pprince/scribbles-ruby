Nokogiri is an XML parsing library for Ruby
-------------------------------------------

```
sudo apt-get install build-essential patch ruby-dev zlib1g-dev liblzma-dev libgmp-dev
gem install --user-install nokogiri
```
